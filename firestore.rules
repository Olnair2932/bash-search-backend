rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Bloqueia acesso publico via navegador
    // O seu Backend no Render continua com acesso total
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
