~/bash/backend $ cat index.js                 const { GoogleGenerativeAI } = require("@google/generative-ai");                            const express = require("express");           const cors = require("cors");                 const axios = require("axios");                                                             const app = express();                        app.use(cors());                              app.use(express.json());                                                                    const PORT = process.env.PORT || 10000;
const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);
                                              app.get("/", (req, res) => res.send("BashSearch API v1.8 - SRE Blindada Online"));                                                        app.post("/search", async (req, res) => {         const { prompt } = req.body;                  try {                                             const model = genAI.getGenerativeModel({                                                        model: "gemini-3.1-flash-lite",                    systemInstruction: "Você é o Sentinela-Bash IA. Retorne o comando Termux precedido por $, uma explicação simples e como instalar."                                                  });                                           const result = await model.generateContent(prompt);                                         res.json({ resposta: result.response.text() });                                         } catch (e) {                                     console.error("ERRO GEMINI:", e.message);                                                   res.status(500).json({ error: "Erro na IA" });
    }                                         });                                                                                         app.listen(PORT, () => console.log(`Servidor v1.8 na porta ${PORT}`));                                                                    setInterval(() => {                               axios.get("https://bash-search-backend.onrender.com").catch(() => {});                  }, 840000);                                   ~/bash/backend $
